syntax="proto3";    //设置版本号
option java_outer_classname="StudentPOJO";   //生成的外部类名，同时也是文件名

message Student {   //会在这个外部类中生成一个内部类Student，它才是真正发送的POJO对象
    int32 id = 1;  //  Student类中一个属性，名叫id，类型为int32,1表示属性序号而不是值
    string name = 2;
}